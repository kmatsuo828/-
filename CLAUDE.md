# マツオ情報ポータル

`zaiko-kanri.html` 単体で動く在庫管理・見積・請求アプリ。バックエンドは Supabase。

## 見積書の作り方（最重要）

**見積書はこのポータルで作る。エクセルで作らない。**

「見積書を作成して」「見積を出して」と言われたら、成果物は XLSX ではなく
**ポータルに投入できる形のデータ**を渡すこと。

- `importQuoteData({title, customer, staff, date, footerNote, lines:[...]})`
  — 見積モーダルを開いて全明細をセットする（`zaiko-kanri.html`）
- `#quote=<base64url(JSON)>` 付きURLで開く
  — 同じJSONをURLで渡す。スマホでコンソールを開かずに済む
- 見積依頼のメモやスクショがあるなら、モーダルの **📷 カメラ / 🖼 アルバム**
  にそのまま渡せば AI が明細を読み取る

`mitsumori-jido-sakusei` スキルは XLSX + G:ドライブ保存の旧手順のまま。
このリポジトリの案件では**そちらに従わない**。

## 2つの「ルール」を混同しない

| | 対象 | 内容 |
|---|---|---|
| 価格表の編集作成ルール | 商品マスタの**売値を登録するとき** | ルームエアコン：定価あり→`仕入 + 定価×3%`／オープン価格→`仕入×1.1`、いずれも千円未満切上。全メーカー共通 |
| 見積書作成ルール | **見積書を出すとき** | マスタの売値をそのまま単価に使う。消費税は `Math.floor(小計×0.1)` 切捨て |

得意先向けのルームエアコン価格表の数字は**参考値として使ってよい**が、
上の価格表編集ルールを見積書に適用しないこと。

## セッションを引き継ぐとき

`.claude/skills/hikitsugi/` に標準手順がある。引き継ぎメモだけで終わらせず、
**次セッションにそのまま貼れるプロンプトと、環境の事前確認を指示する一文まで必ず出す。**

## 仕入先の価格表を取り込むとき

`.claude/skills/shiiresaki-kakakuhyo-torikomi/` に手順をまとめてある。
メール添付・ドライブ上のPDF/XLSX から一括登録CSVまで、メーカーを問わず同じ流れ。

## 掛率

ポータルの価格ルール（`DEFAULT_PRICE_RULES`）にメーカー／ジャンル／シリーズ／品番接頭辞別の
既定値が入っている。具体度順（品番8点・シリーズ4点・メーカー2点・ジャンル1点）で
最初に計算可能なルールが適用される。

ルール未登録のメーカーは手入力。例：因幡電工スリムダクトは **SD本体のみ55%、付属品（SW・SJ・SK・SC・SKF・SF等）は一律60%**。

## 環境の制約（クラウドセッション時）

**制約は思い込みで判断せず、必ず実際に叩いて確かめてから報告すること。**

クラウド実行でも **ブラウザは使える**。Chromium と Playwright が入っている
（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）。ポータルを
`http-server` でローカル配信すれば、`#quote=` リンクで投入 → `generateQuotePdf()` →
`@media print` 経由で PDF まで出せる。実証済み。**「ブラウザが無いから」は理由にならない。**

使えないのは外部への通信で、原因はサイト側ではなく**送信元プロキシの許可リスト**。

| 対象 | 可否 | 症状 |
|---|---|---|
| `127.0.0.1`（ローカル配信のポータル） | ○ | — |
| Gmail / Google ドライブの**MCP連携** | ○ | 検索・本文・ファイル読取は可能 |
| `*.supabase.co` | × | CONNECT に 403。商品マスタ・在庫は照会不可 |
| `mail.google.com` / `drive.google.com` | × | HTTP 000。ブラウザでも同じ |
| メーカー・販売店サイト（`inaba-denko.com` 等） | × | `ERR_TUNNEL_CONNECTION_FAILED`。定価確認は不可 |
| WebSearch | ○ | 検索結果は取れるがページ本文は取れない |

- Gmail連携に添付ダウンロードのRPCが無い。添付の中身が要るときはドライブ保存か直接アップロードを依頼する
- G:ドライブ（Google Drive for Desktop）はマウントされない
- `libreoffice-calc` は未インストール。XLSX→PDF 変換が要るなら先に入れる

できないことが分かったら、**代替に走る前にその場で伝える**。特に XLSX 作成へ
勝手に切り替えないこと。
